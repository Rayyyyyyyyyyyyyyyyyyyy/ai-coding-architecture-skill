# Collection skill prefix

Add the consistent `vibe-` prefix to the eight short skill names. Rename skill
and current evaluation directories, frontmatter names, display titles, invocation
prompts, references, README examples, and current routing documentation.
Preserve behavior, historical run snapshots, and installed global copies.
