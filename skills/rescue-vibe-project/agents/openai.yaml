interface:
  display_name: "Rescue Vibe Project"
  short_description: "Restore one trusted slice of a brittle AI-built project"
  default_prompt: "Use $rescue-vibe-project to establish the real baseline, present an issue checklist, and wait for my agreement on the repair scope before restoring one verified, high-value vertical slice."

policy:
  allow_implicit_invocation: false
