interface:
  display_name: "Coding Architecture"
  short_description: "Keep frontend architecture maintainable and composable"
  default_prompt: "Use $coding-architecture to assess the frontend boundaries or implement the requested change using existing conventions and primitives; keep advice-only requests read-only."
