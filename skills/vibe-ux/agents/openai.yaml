interface:
  display_name: "Vibe UX"
  short_description: "Complete only the user states a flow actually signals"
  default_prompt: "Use $vibe-ux to identify this flow's relevant user-visible states; report a checklist for assessment requests and complete states only when implementation is requested."
